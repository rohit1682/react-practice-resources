const About = () => {
  return (
    <div>
      <h1>About Page</h1>
      <p>This is the about page of our React Router demo application.</p>
      <p>
        React Router is a standard library for routing in React applications. It
        enables navigation among views in your React application, allowing you
        to build a single-page web application with navigation without the page
        refreshing.
      </p>
    </div>
  );
};

export default About;
